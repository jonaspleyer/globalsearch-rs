//! # Local Solver module.
//!
//! This module contains the interface for `argmin`'s local solvers, which are used to solve optimization problems locally.
//! It also contains the builders needed to create and configure the local solvers.

pub mod builders;
pub mod runner;
